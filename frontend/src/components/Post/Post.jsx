import React from 'react';
import css from './Post.module.scss';

const Post = () => {
    return (
        <section className="css.card">
            
        </section>
    );
};

export default Post;